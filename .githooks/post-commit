#!/bin/bash
# Windows compatibility: call batch or PowerShell script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
if command -v powershell.exe &> /dev/null; then
    powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$SCRIPT_DIR/auto_push.ps1"
else
    bash "$SCRIPT_DIR/auto_push.sh"
fi